   - created_at
   - updated_at

7. jwt_tokens table:
   - id
   - user_id
   - token
   - created_at
   - expires_at

Make sure to use environment variables for sensitive information such as JWT secret and DB credentials.